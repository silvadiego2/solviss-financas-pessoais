/**
 * @deprecated Use `useBusinessValidation` — que já inclui checkAccountDependencies
 * e checkCategoryDependencies. Este arquivo existe apenas para não quebrar imports antigos.
 */
export { useBusinessValidation as useDependencyCheck } from './useBusinessValidation';
